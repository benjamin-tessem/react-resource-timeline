// Default cell height for resources, and timeline items
export const DEFAULT_CELL_HEIGHT = 50;

// Width for 30 minute intervals, to make it easy, it is currently 30px, 1px per minute
export const DEFAULT_COLUMN_WIDTH = 30;

export const DEFAULT_CAL_WIDTH = 1440; // 24 hours, 1 minute = 1px

export const DEFAULT_DAY_LENGTH = 24 * 2; // 24 hours, columns are 30 minutes, so 48 columns
